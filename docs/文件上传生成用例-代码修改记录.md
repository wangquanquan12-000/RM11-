# 文件上传生成用例 · 代码修改记录

> **对应 PRD**：`docs/文件上传生成用例-PRD.md`  
> **修改日期**：2025-03-02

---

## 一、修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `crew_test.py` | 新增 + 修改 | `parse_uploaded_files`、`export_tables_to_excel_bytes`；常量 `_MAX_*_BYTES` |
| `pipeline_service.py` | 新增 + 修改 | `run_upload_to_cases`、`UploadToCasesResult`；导入 `export_tables_to_excel_bytes` |
| `app_ui.py` | 修改 | 需求来源切换（Quip \| 文件上传）、`_render_upload_mode` |

---

## 二、具体修改内容

### 2.1 crew_test.py

**新增**：

```python
# 文件上传大小限制（PRD 安全规范）
_MAX_SINGLE_FILE_BYTES = 10 * 1024 * 1024  # 10MB
_MAX_TOTAL_UPLOAD_BYTES = 50 * 1024 * 1024  # 50MB

def parse_uploaded_files(uploaded_files: list) -> tuple[str, str, list[dict[str, Any]]]:
    """解析上传的 .md 与 .xlsx 文件..."""

def export_tables_to_excel_bytes(tables: list[list[list[str]]]) -> bytes | None:
    """将表格导出为 Excel 二进制，供 st.download_button 使用。"""
```

- `parse_uploaded_files`：按扩展名区分 .md / .xlsx，校验大小，返回（需求合并、既有用例合并、预览信息）
- `export_tables_to_excel_bytes`：复用 `_sanitize_cell_for_excel` 防公式注入，写入 `BytesIO` 后返回 `bytes`

### 2.2 pipeline_service.py

**新增**：

```python
class UploadToCasesResult(TypedDict):
    ok: bool
    error: str | None
    understanding: str
    issues: str
    cases_md: str
    excel_bytes: bytes | None

def run_upload_to_cases(
    demand_md: str,
    existing_cases: str,
    gemini_key: str,
    gemini_model: str,
    project_context: str = "",
) -> UploadToCasesResult:
```

- 将既有用例拼入 `project_context`
- 调用 `run_pipeline(..., export_excel=False, export_quip=False, export_sheets=False, return_details=True)`
- 从 `step_outputs` 提取 task1→问题点、task2→理解内容、task3→新用例表
- 用 `_parse_markdown_tables` + `export_tables_to_excel_bytes` 生成 Excel 二进制

### 2.3 app_ui.py

**修改**：

- `_render_module_quip_to_cases` 顶部增加 `st.radio` 需求来源：`Quip 链接` \| `文件上传（.md + .xlsx）`
- 选择「文件上传」时调用 `_render_upload_mode` 并 `return`

**新增** `_render_upload_mode`：

- `st.file_uploader(..., type=["md", "xlsx"], accept_multiple_files=True)`
- 使用 `parse_uploaded_files` 解析并展示预览
- 配置区仅「模型」；说明「仅支持 Excel 下载」
- 调用 `run_upload_to_cases` 执行流水线
- 结果分三块：1. 理解内容  2. 问题点  3. 新测试用例表
- `st.download_button` 下载 Excel

---

## 三、Code Review 要点

### 3.1 安全性

| 项 | 结论 |
|----|------|
| 凭证硬编码 | ✅ 无；Gemini Key 来自 defaults/env |
| 文件类型白名单 | ✅ 仅接受 .md、.xlsx |
| 大小限制 | ✅ 单文件 10MB、总 50MB |
| Excel 注入 | ✅ 使用 `_sanitize_cell_for_excel` |
| 目录遍历 | ✅ 文件在内存处理，不落盘 |

### 3.2 业务规范

| 项 | 结论 |
|----|------|
| 至少 1 个 .md | ✅ `parse_uploaded_files` 校验；无 .md 则 `demand_md` 为空 |
| 仅 Excel 下载 | ✅ 无导出路径、Quip、Sheets 配置 |
| 三块结果 | ✅ 理解内容(task2)、问题点(task1)、新用例表(task3) |
| 既有用例注入 | ✅ 拼入 `project_context` 传入流水线 |

### 3.3 回归风险

| 功能 | 影响 |
|------|------|
| Quip 模式 | ❌ 不改动 |
| 项目记忆 | ❌ 不改动 |
| 编辑 Agent | ❌ 不改动 |
| 需求风险分析 | ❌ 不改动 |

---

## 四、本地验证命令

```bash
# 1. 解析与导出函数
python -c "
from crew_test import parse_uploaded_files, export_tables_to_excel_bytes
from io import BytesIO
class F:
    def __init__(self, name, data):
        self.name = name
        self._data = data
    def read(self):
        return self._data
files = [F('req.md', b'# 需求\n\n用户登录')]
d, e, p = parse_uploaded_files(files)
print('OK:', bool(d), len(p))
"

# 2. run_upload_to_cases（未配置 Key 时返回错误）
python -c "
from pipeline_service import run_upload_to_cases
r = run_upload_to_cases('x', '', '', 'gemini-2.5-flash-lite')
print('OK:', not r['ok'], 'error' in str(r))
"

# 3. UI 启动
streamlit run app_ui.py
# 进入「生成用例」→ 选择「文件上传（.md + .xlsx）」→ 上传文件 → 开始生成
```

---

## 五、验收标准对照

| AC | 验收项 | 实现 |
|----|--------|------|
| AC1 | 支持一次上传多个 .md 和 .xlsx 文件（可混合） | ✅ `accept_multiple_files=True`, `type=["md","xlsx"]` |
| AC2 | .md 正确解析为文本，.xlsx 正确解析为表格内容 | ✅ `parse_uploaded_files` |
| AC3 | 结果分为三块：理解内容 / 问题点 / 新用例表 | ✅ `_render_upload_mode` |
| AC4 | 提供「下载 Excel」按钮 | ✅ `st.download_button` |
| AC5 | 不提供导出路径、Quip、Sheets 配置 | ✅ 上传模式无上述选项 |
| AC6 | 至少需 1 个 .md；非法类型或空文件有提示 | ✅ 校验与 `st.warning` |

---

**✅ 本地检查与 Code Review 已完成，可提交代码。**
