# 产品审查：@编程 近期改动

> **角色**：@产品  
> **审查对象**：@编程 的 bug 修复、偏差修正及实现状态  
> **依据**：`implementation-handoff-for-programming.md`、`ui-architecture-spec.md`

---

## 一、审查结论概览

| 维度 | 结论 | 说明 |
|------|------|------|
| **偏差修正** | ✅ 已纠正 | 主区顶部快捷导航已移除，符合信息架构 |
| **配置驱动** | ✅ 已完成 | 高优先级文案已全部迁入 ui_texts.yaml 并改用 _get_text |
| **实施清单** | ✅ 部分完成 | 3.2 成功卡片化、3.3 失败卡片化已实现；3.1 最近 5 条暂缓 |
| **Bug 修复** | ✅ 合理 | 持久化、进度、Agent 理解展示等符合体验方向 |

---

## 二、修改建议（按优先级）

### 高优先级：配置驱动合规

**规范**：ui-architecture-spec 2.1 要求「不写死文案」。以下应迁入 `config/ui_texts.yaml`：

| 当前硬编码 | 建议 key | 建议默认值 |
|------------|----------|------------|
| `"去设置"` | `run_tab.go_settings` | 去设置 |
| `"← 返回"` | `app.back_btn` | ← 返回 |
| `"拉取中…"` | `memory_tab.import_spinner_quip` | 拉取中… |
| `"解析文件中…"` | `memory_tab.import_spinner_file` | 解析文件中… |
| `"请填写上方 Quip Token"` | `memory_tab.quip_token_required` | 请填写上方 Quip Token |
| `"请填写 Quip 文档链接"` | `memory_tab.quip_url_required` | 请填写 Quip 文档链接 |
| `"文件内容为空"` | `memory_tab.file_empty` | 文件内容为空 |
| `"解析失败"` | `memory_tab.parse_fail` | 解析失败 |
| `"请上传文件或粘贴内容"` | `memory_tab.import_required` | 请上传文件或粘贴内容 |
| `"保存摘要"` | `memory_tab.save_summary_btn` | 保存摘要 |
| `"从本次运行追加"` | `memory_tab.append_from_run_btn` | 从本次运行追加 |
| `"请先在「生成用例」页运行一次"` | `memory_tab.run_first_hint` | 请先在「生成用例」页运行一次 |
| `"✓ 全回归用例已导入（X 字）…"` | `memory_tab.full_regression_status` | 需支持占位符，如 `全回归用例已导入（{count} 字），生成用例时 Agent 将参考理解。` |

**操作**：@编程 在 `ui_texts.yaml` 中新增上述 key，代码中改为 `_get_text(T, "key")`。

**✅ 已实现**（2026-02-28）：所有 key 已加入 ui_texts.yaml，app_ui.py 中对应硬编码已替换为 _get_text。

---

### 中优先级：实施清单补全

| 项 | 交接单要求 | 当前状态 | 建议 |
|----|------------|----------|------|
| **3.1 最近生成 5 条** | 最多 5 条，每条：标题、时间、[打开 Excel][归档] | 仅 1 条 | **暂缓**：3.1 暂缓，待历史列表持久化方案（或从 output/ 扫描最近 5 个 crew_result_*） |
| **3.2 成功结果卡片化** | 卡片形式展示标题、路径、下载/打开按钮 | ✅ 已实现 | 用 `.card-style` 卡片展示标题、路径，下方为下载/打开按钮 |
| **3.3 失败结果卡片化** | 卡片 + 重试按钮 | ✅ 已实现 | 失败时展示 `.card-style` 卡片，内含错误信息 +「重试」按钮 |

---

### 低优先级：侧栏收起体验

**背景**：用户曾反馈「侧栏收起后没有位置可以重新打开」，@编程 曾加主区顶部导航，后按设计移除。

**产品建议**：  
- 当前：依赖 Streamlit 默认左上角 ◀ 展开侧栏。  
- 若用户继续反馈「找不到展开入口」，可考虑：在**主区顶部或页面左上角**增加一个**轻量提示**（如 `st.caption("侧栏已收起？点左上角 ◀ 展开工作台")`），文案迁入 ui_texts，且可配置为隐藏。  
- 不在主区做完整导航栏，仅保留提示，兼顾设计与体验。

---

## 三、已做得好的部分

1. **last_run 持久化**：刷新后仍可展示最近生成，符合用户预期。  
2. **进度条**：需求拉取 → 四 Agent 执行 → 完成，步骤清晰。  
3. **项目记忆 - 全回归**：导入 spinner +「已导入（X 字），Agent 将参考」状态展示，反馈明确。  
4. **设置页「← 返回」**：合理补全，建议保留并将文案配置化。  
5. **render_log_terminal key 修复**：解决 Cloud 报错，符合规范。  
6. **偏差修正**：主区顶部导航已移除，信息架构与设计一致。

---

## 四、交接单与红线的澄清

- **登录/注册移除**：由用户明确要求，与交接单「不得削弱」不冲突；若后续需恢复，再按新方案实现。  
- **五、功能回退红线**：建议在交接单中注明「登录已按用户要求移除，其余不得削弱」。

---

## 五、给 @编程 的行动清单

1. **必须**：将第二节「高优先级」中的硬编码文案迁入 `ui_texts.yaml` 并改用 `_get_text`。 **✅ 已完成**
2. **建议**：实现 3.2、3.3 成功/失败卡片化（本期或下一迭代）。 **✅ 已完成**
3. **可选**：3.1 最近生成 5 条、侧栏收起提示，与产品确认排期后实现。

---

## 六、实现记录（@编程 更新）

| 日期 | 内容 |
|------|------|
| 2026-02-28 | 高优先级：12 项文案迁入 ui_texts.yaml，app_ui.py 中全部改用 _get_text |
| 2026-02-28 | 中优先级：3.2 成功结果卡片化（.card-style 展示标题、路径、下载/打开按钮）；3.3 失败结果卡片化（.card-style 错误卡 + 重试按钮） |

---

**文档版本**：1.1  
**维护**：@产品
